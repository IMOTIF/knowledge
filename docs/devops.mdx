---
title: Modern DevOps Workflow and AI 
description: Learn Git, GitHub, Docker, CI/CD, and deployment through hands-on practice.
sidebar_position: 4

---

## Intro & Overview

ในโลกการพัฒนาซอฟต์แวร์จริง การเขียนโค้ดให้รันได้ในเครื่องตัวเองยังไม่ถือว่างานเสร็จ
โค้ดหนึ่งชุดต้องผ่านหลายขั้นตอน ตั้งแต่การพัฒนา การทดสอบ ไปจนถึงการนำไปใช้งานจริง (Production)

การอบรมนี้ออกแบบมาเพื่อให้น้อง ๆ เข้าใจ **ภาพรวมของการทำงานแบบทีม**
และเห็นเส้นทางของโค้ดตั้งแต่วันแรกที่เขียน จนถึงวันที่ผู้ใช้งานสามารถเข้าถึงได้จริง

## Git

Git คือระบบควบคุมเวอร์ชัน (Version Control System)
ที่ใช้จัดการการเปลี่ยนแปลงของโค้ด และช่วยให้หลายคนทำงานร่วมกันได้อย่างเป็นระบบ

Git ทำงานอยู่ในเครื่องของผู้พัฒนา และสามารถใช้งานได้แม้ไม่เชื่อมต่ออินเทอร์เน็ต

## Continuous Integration (CI)

CI คือกระบวนการตรวจสอบโค้ดอัตโนมัติทุกครั้งที่มีการเปลี่ยนแปลง
เช่น การรัน test หรือการตรวจรูปแบบโค้ด เพื่อให้มั่นใจว่าโค้ดใหม่ไม่ทำให้ระบบเดิมพัง

ในคอร์สนี้จะใช้ **GitHub Actions** เป็นเครื่องมือ CI
โดยเมื่อมีการ push โค้ด ระบบจะรัน **Jest tests** ให้อัตโนมัติ

## Continuous Deployment (CD)

CD คือขั้นตอนนำโค้ดที่ผ่านการตรวจสอบแล้วไป deploy
อาจเป็นการ deploy แบบกดเอง หรือแบบอัตโนมัติ

ในการอบรมนี้จะใช้ **Docker** และ **Watchtower**
เพื่อแสดงให้เห็นแนวคิดของการ deploy ที่ลดขั้นตอน manual และลดความผิดพลาดจากมนุษย์

## การเริ่มต้นใช้งาน Git

#### `git init`

ใช้สำหรับเริ่มต้น repository ใหม่ในโฟลเดอร์ปัจจุบัน

```bash
git init
```

เหมาะสำหรับโปรเจกต์ที่เริ่มจากศูนย์

```html
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>

<body>
  Hello world
</body>

</html>
```

#### `git clone`

ใช้สำหรับดึง repository ที่มีอยู่แล้วมาจาก remote

```bash
git clone <repository-url>
```

<img
  src="/img/git-clone.gif"
  alt="Git Clone"
  style={{ width: "100%", maxWidth: "900px" }}
/>

เหมาะสำหรับโปรเจกต์ที่ทำงานเป็นทีม หรือโปรเจกต์จาก GitHub

## Git command พื้นฐาน

#### `git add`

เลือกไฟล์ที่ต้องการบันทึกการเปลี่ยนแปลง

```bash
git add .
```

#### `git commit`

บันทึกสถานะของโค้ด ณ เวลานั้น พร้อมข้อความอธิบาย

```bash
git commit -m "Add login feature"
```

ข้อความ commit ควรอธิบายว่า “ทำอะไร” ไม่ใช่ “แก้อะไรไปบ้าง”

#### `git log`

แสดงประวัติการ commit ทั้งหมด

```bash
git log
```

## Branch เบื้องต้น

Branch คือเส้นทางการพัฒนาแยกออกจากโค้ดหลัก
ช่วยให้สามารถทดลองหรือพัฒนา feature ใหม่ได้โดยไม่กระทบโค้ดหลัก

### สร้าง branch ใหม่

```bash
git branch feature/login
```

### สลับไปยัง branch

```bash
git checkout feature/login
```

หรือ

```bash
git switch feature/login
```

## Branch Strategy (main / dev / feature)

ในการทำงานเป็นทีม โค้ดทั้งหมดไม่ควรถูกแก้ไขบน branch เดียวกัน
การมี **Branch Strategy** ที่ชัดเจนจะช่วยลดความเสี่ยงที่โค้ดหลักจะพัง และทำให้ทีมทำงานร่วมกันได้อย่างเป็นระบบ

โครงสร้าง branch ที่ใช้ในคอร์สนี้ประกอบด้วย:

* **main**
  ใช้เก็บโค้ดที่พร้อมใช้งานจริง (Production-ready)
  ไม่ควรแก้ไขหรือทดลองโค้ดบน branch นี้โดยตรง

* **dev**
  ใช้เป็นพื้นที่รวมงานจากหลาย feature เพื่อทดสอบร่วมกัน
  ก่อนนำโค้ดไปใช้งานจริง

* **feature/***
  ใช้สำหรับพัฒนา feature หรือแก้ไขงานเฉพาะส่วนของแต่ละคน
  โดยแยกออกจากโค้ดหลักอย่างชัดเจน

แนวคิดหลักคือ

> ไม่ทำงานบน main
> ทุก feature ต้องผ่าน dev ก่อน

## Repository & Issue

**Repository** คือพื้นที่เก็บโค้ดและประวัติการเปลี่ยนแปลงทั้งหมดของโปรเจกต์
บน GitHub หนึ่ง repository มักประกอบด้วย:

* source code
* ประวัติ commit
* branch ต่าง ๆ
* เอกสารและการตั้งค่า CI/CD

**Issue** ใช้สำหรับจัดการงานและปัญหาในโปรเจกต์ เช่น:

* รายงาน bug
* ขอ feature ใหม่
* พูดคุยหรือเสนอแนวทางแก้ไข

การใช้ issue ช่วยให้ทีม:

* เห็นภาพรวมของงานทั้งหมด
* รู้ว่าใครกำลังทำอะไร
* ลดการสื่อสารที่หลุดจากบริบทของโค้ด

## Pull Request (PR) คืออะไร

**Pull Request (PR)** คือกระบวนการขอให้ทีมตรวจสอบโค้ด
ก่อนนำโค้ดจาก branch หนึ่งไป merge เข้าสู่อีก branch หนึ่ง

PR ช่วยให้:

* มีการตรวจสอบโค้ด (Code Review)
* ลดความผิดพลาดก่อน merge
* เกิดการแลกเปลี่ยนความรู้ในทีม

กระบวนการพื้นฐานของ PR:

1. พัฒนา feature บน branch ของตัวเอง
2. push โค้ดขึ้น GitHub
3. เปิด Pull Request
4. ให้เพื่อนร่วมทีม review
5. แก้ไขตาม feedback
6. merge เมื่อโค้ดผ่านการตรวจสอบ

> Pull Request ไม่ใช่แค่ขั้นตอนทางเทคนิค
> แต่เป็นเครื่องมือสำคัญในการทำงานเป็นทีมอย่างมีคุณภาพ

<img
  src="/img/pr.gif"
  alt="Pull Request"
  style={{ width: "100%", maxWidth: "900px" }}
/>

### ขั้นตอนการเริ่มต้นใช้งาน Git และอัปโหลดโค้ดไปยัง GitHub

<img
  src="/img/new-repo-step-1.png"
  alt="New Repository Step 1"
  style={{ width: "100%", maxWidth: "900px" }}
/>

<img
  src="/img/new-repo-step-2.gif"
  alt="New Repository Step 2"
  style={{ width: "100%", maxWidth: "900px" }}
/>

1. **สร้างไฟล์ `app.js`** และเขียนฟังก์ชันพื้นฐานดังนี้

```js
function sum (a, b) {
  return a + b
}
```

2. **เริ่มต้นใช้งาน Git ภายในโปรเจกต์**

```bash
git init
```

3. **เพิ่มไฟล์ทั้งหมดเข้าสู่ Staging Area**

```bash
git add .
```

4. **บันทึกการเปลี่ยนแปลง (Commit) ลงใน Git**

```bash
git commit -m "Add function calculate"
```

5. **เชื่อมต่อ Repository กับ GitHub และอัปโหลดโค้ด**

```bash
git branch -M main
git remote add origin https://github.com/tao-thewarat/ci-playground.git
git push -u origin main
```

<img
  src="/img/push-code.png"
  alt="Push code"
  style={{ width: "100%", maxWidth: "900px" }}
/>

## push code และเปิด Pull Request

หลังจากพัฒนา feature เสร็จบน branch ของตัวเอง
ขั้นตอนถัดไปคือการนำโค้ดขึ้น GitHub เพื่อขอให้ทีมตรวจสอบ

กระบวนการทำงานโดยทั่วไปมีดังนี้:

1. ตรวจสอบว่าอยู่บน branch ที่ถูกต้อง
2. commit โค้ดในเครื่องของตัวเอง
3. push โค้ดขึ้นไปยัง GitHub
4. เปิด Pull Request ไปยัง branch เป้าหมาย (เช่น `dev`)

Pull Request ทำหน้าที่เป็นพื้นที่กลางสำหรับ:

* แสดงการเปลี่ยนแปลงของโค้ด
* อธิบายว่าโค้ดนี้ทำอะไร
* ให้ระบบ CI ตรวจสอบโค้ดอัตโนมัติ

การเปิด Pull Request แทนการ merge ตรง
ช่วยให้ทีมสามารถตรวจสอบและพูดคุยเกี่ยวกับโค้ดได้ก่อนนำไปใช้งานจริง

```js
function sum(a, b) {
  return a + b
}

module.exports = sum
```

สร้าง branch ใหม่
```bash
git checkout -b feature/multiply
```

เพื่ม function multiply
```js
function sum(a, b) {
  return a + b
}

function multiply(a, b) {
  return a * b
}

module.exports = { sum, multiply }
```

push code เข้า branch ที่สร้างขึ้นมาใหม่

<img
  src="/img/push-on-branch.png"
  alt="Push code on branch"
  style={{ width: "100%", maxWidth: "900px" }}
/>

แล้วเข้ามาในหน้า repo ใน Github จะเจอปุ่ม pr แสดงมา
<img
  src="/img/push-on-branch.gif"
    alt="Pull request on github"
  style={{ width: "100%", maxWidth: "900px" }}
/>

## review และ merge

เมื่อมีการเปิด Pull Request
เพื่อนร่วมทีมจะเข้ามาตรวจสอบโค้ดใน Pull Request นั้น

การ review โค้ดมักเน้นดูว่า:

* โค้ดทำงานตรงตามที่ตั้งใจหรือไม่
* มีจุดที่อาจเกิดปัญหาหรือควรปรับปรุงหรือไม่
* โค้ดอ่านเข้าใจง่ายและดูแลต่อได้หรือไม่

หลังจากโค้ดผ่านการ review และระบบ CI ตรวจสอบเรียบร้อยแล้ว
จึงสามารถ merge โค้ดเข้าสู่ branch เป้าหมายได้อย่างปลอดภัย

การ merge ผ่าน Pull Request ช่วยให้:

* ลดความเสี่ยงของ bug หลุดเข้าโค้ดหลัก
* ทุกการเปลี่ยนแปลงมีการตรวจสอบก่อนใช้งาน
* ทีมทำงานร่วมกันได้อย่างเป็นระบบมากขึ้น

## CI ทำงานตอนไหน และทำไมต้องมี

CI (Continuous Integration) คือกระบวนการตรวจสอบโค้ดอัตโนมัติ
ทุกครั้งที่มีการเปลี่ยนแปลงโค้ดในระบบควบคุมเวอร์ชัน เช่น GitHub

เป้าหมายของ CI ไม่ใช่การแทนที่มนุษย์
แต่เป็นการช่วยตรวจจับปัญหาให้เร็วที่สุด ก่อนที่โค้ดจะถูกรวมเข้ากับโค้ดหลักของทีม

### CI ทำงานตอนไหน

โดยทั่วไป CI จะทำงานเมื่อเกิดเหตุการณ์เหล่านี้:

* มีการ **push code** ขึ้น repository
* มีการ **เปิด Pull Request**
* มีการ **แก้ไขโค้ดใน Pull Request**
* (บางระบบ) ทำงานตามเวลา เช่น ทุกคืน

ในคอร์สนี้ CI จะทำงานเมื่อ:

* push โค้ดขึ้น branch `dev` หรือ `main`
* เปิดหรืออัปเดต Pull Request

ทันทีที่เหตุการณ์เกิดขึ้น ระบบ CI จะเริ่มรันขั้นตอนที่กำหนดไว้ เช่น:

* ติดตั้ง dependencies
* รัน unit test
* รายงานผลว่าโค้ด “ผ่าน” หรือ “ไม่ผ่าน”

### ทำไมต้องมี CI

ถ้าไม่มี CI การตรวจสอบโค้ดจะต้องพึ่งมนุษย์ทั้งหมด
ซึ่งมักเกิดปัญหาเหล่านี้:

* ลืมรัน test ก่อน merge
* โค้ดบางส่วนพังโดยไม่รู้ตัว
* รู้ว่าโค้ดพังตอนที่ระบบรันจริงแล้ว

CI ช่วยแก้ปัญหาเหล่านี้โดย:

* ตรวจสอบโค้ดทุกครั้งแบบอัตโนมัติ
* แจ้งเตือนทันทีเมื่อมีปัญหา
* ป้องกันโค้ดที่ยังไม่พร้อมจากการถูก merge

<img
  src="/img/ci-check.png"
  alt="CI Check"
  style={{ width: "100%", maxWidth: "900px" }}
/>

## เริ่มเขียน Unit Test ด้วย Jest

ในหัวข้อนี้เราจะเขียน **Unit Test แรก**
เพื่อทดสอบฟังก์ชันอย่างง่าย และเตรียมพร้อมสำหรับการนำไปใช้กับ CI

### ขั้นตอนการเขียน Unit Test

1. ที่ root ของโปรเจกต์ ให้สร้างโฟลเดอร์ชื่อ `tests`
2. ภายในโฟลเดอร์ `tests` ให้สร้างไฟล์ชื่อ `app.test.js`
3. เขียน test case ด้วย Jest ดังตัวอย่างด้านล่าง

```js
const sum = require('../app')

test('adds two numbers correctly', () => {
  expect(sum(1, 2)).toBe(3)
})
```

ในตัวอย่างนี้:

* `require('../app')` ใช้สำหรับนำฟังก์ชันที่ต้องการทดสอบเข้ามา
* `test()` คือหนึ่ง test case
* `expect()` ใช้ตรวจสอบผลลัพธ์ที่ได้
* หากค่าที่ได้ตรงกับที่คาดหวัง test จะผ่านทันที

### โครงสร้างไฟล์ตัวอย่าง

```text
.
├── app.js
└── tests
    └── app.test.js
```

### รัน Unit Test ในเครื่อง

หลังจากเขียน test เสร็จแล้ว สามารถรัน test ได้ด้วยคำสั่ง:

```bash
npm test
```

หรือ

```bash
npx jest
```

หาก test ผ่าน จะเห็นผลลัพธ์แสดงว่า test สำเร็จ
ซึ่งเป็นพื้นฐานสำคัญก่อนนำ test ไปใช้ในระบบ CI

<img
  src="/img/result-tests.png"
  alt="Result tests"
  style={{ width: "100%", maxWidth: "900px" }}
/>

## เริ่มต้นเขียน CI ด้วย GitHub Actions

เพื่อให้ GitHub สามารถรัน CI อัตโนมัติได้
จำเป็นต้องสร้างไฟล์ workflow ตามโครงสร้างที่ GitHub กำหนดไว้

### ขั้นตอนการเตรียมไฟล์ CI

1. ที่ root ของโปรเจกต์ ให้สร้างโฟลเดอร์ชื่อ `.github`
2. ภายในโฟลเดอร์ `.github` ให้สร้างโฟลเดอร์ย่อยชื่อ `workflows`
3. ภายในโฟลเดอร์ `workflows` ให้สร้างไฟล์ชื่อ `tests.yaml`

โครงสร้างไฟล์จะมีลักษณะดังนี้:

```text
.
└── .github
    └── workflows
        └── tests.yaml
```

ไฟล์ `tests.yaml` จะใช้สำหรับกำหนดขั้นตอนการทำงานของ CI
เช่น การติดตั้ง dependencies และการรัน unit test แบบอัตโนมัติ

```yaml
name: tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Run unit tests
        run: npx jest
```
หลังจากเปิด Pull Request ขึ้นมาแล้ว
GitHub จะเริ่มรันระบบ CI (Continuous Integration) อัตโนมัติทันที

<img
  src="/img/ci-tests.png"
  alt="CI tests"
  style={{ width: "100%", maxWidth: "900px" }}
/>

## Docker Container

Docker คือเครื่องมือที่ช่วยให้เราสร้างสภาพแวดล้อมสำหรับรันโปรเจกต์ได้ง่าย ๆ เหมือนยัดทุกอย่างที่โปรเจกต์ต้องใช้ — เช่น ภาษา, ฐานข้อมูล, ไลบรารี — ไว้ในกล่องเดียว (ที่เรียกว่า Container)  
พูดง่าย ๆ คือ ไม่ว่าจะเอาไปเปิดที่เครื่องไหน มันก็รันได้เหมือนกันเป๊ะ ไม่ต้องมานั่งลงโปรแกรมหรือเซ็ตระบบใหม่ทุกครั้ง

สมัยที่ผมเริ่มหัดเขียนโปรแกรมใหม่ ๆ ผมเชื่อว่าหลายคนน่าจะเคยผ่านจุดนั้นเหมือนกัน — ใช้ XAMPP เพื่อรัน MySQL แล้วก็มักจะเจอปัญหาแปลก ๆ เช่น รันอยู่ดี ๆ แล้ว error หรือบางทีต้อง restart เครื่องใหม่ถึงจะใช้ได้อีกครั้ง  

ตอนนั้นผมก็พยายามหาวิธีแก้ปัญหาอยู่พักหนึ่ง จนมีเพื่อนในกลุ่มแนะนำให้ลองใช้ **Docker** ดู ตอนแรกก็ยังงง ๆ ว่ามันคืออะไร ใช้ทำอะไรได้บ้าง แต่พอได้ลองจริง ๆ ก็รู้เลยว่ามันช่วยให้การพัฒนาง่ายขึ้นมาก ตั้งแต่นั้นมาผมก็ใช้ Docker มาตลอดจนถึงตอนนี้  

ส่วนตัวผมมองว่า Docker เป็นเครื่องมือที่ดีมากสำหรับงานพัฒนา เพราะมันช่วยให้เราสามารถสร้างสภาพแวดล้อมสำหรับการเขียนโปรแกรมได้อย่างรวดเร็ว ไม่ต้องมานั่งติดตั้งอะไรซ้ำ ๆ และลดปัญหาที่ว่า “รันได้แค่บนเครื่องเรา” ไปได้เยอะเลย

![XAMPP error](/img/xampp-error.png)


## ทำไมถึงควรใช้ Docker

ก่อนจะเริ่มลงมือใช้ Docker มาดูกันก่อนว่าทำไมเครื่องมือนี้ถึงกลายเป็นของจำเป็นสำหรับนักพัฒนาเกือบทุกคนในยุคนี้

สมัยก่อน เวลาจะรันโปรเจกต์หนึ่ง โปรเจกต์มักจะมี dependency เยอะมาก —
เช่น ต้องใช้ Python เวอร์ชัน 3.10, PostgreSQL 15, Redis 7 และต้องติดตั้ง library เพิ่มอีกเป็นสิบตัว
พอเราจะให้เพื่อนในทีมรันโปรเจกต์เดียวกัน ก็มักจะเจอปัญหาเดิม ๆ อย่าง

* “ของเรารันได้ แต่ของเพื่อนไม่ได้”
* “เครื่องนี้ต้องใช้ Node 18 แต่เครื่องนั้นมี Node 20”
* “พออัปเดต library แล้วโปรเจกต์พัง”

Docker เข้ามาช่วยแก้ปัญหาเหล่านี้ทั้งหมดโดยทำให้ทุกอย่างอยู่ในกล่องเดียว (container)
ไม่ว่าใครจะรันโปรเจกต์นี้ ที่ไหน หรือบนระบบปฏิบัติการอะไร — ผลลัพธ์จะเหมือนกันเป๊ะ

ลองสรุปให้เห็นภาพแบบง่าย ๆ:

| ปัญหาก่อนใช้ Docker        | เมื่อใช้ Docker แล้ว                 |
| -------------------------- | ------------------------------------ |
| “มันรันได้แค่บนเครื่องเรา” | รันได้ทุกเครื่องเหมือนกัน 100%       |
| ต้องลง dependency เอง      | ทุกอย่างอยู่ใน container แล้ว        |
| เสี่ยงพังเวลาอัปเดต        | ใช้ image เดิมได้เสมอ                |
| Setup เครื่องนาน           | แค่ `docker compose up` ก็พร้อมทำงาน |

Docker ทำให้การพัฒนาเป็นเรื่องง่ายและเป็นระบบมากขึ้น โดยเฉพาะเวลาเราทำงานเป็นทีม
ทุกคนจะอยู่บนสภาพแวดล้อมเดียวกันหมด ไม่ต้องมานั่งแก้ “ของใครพังไม่รู้” อีกต่อไป

## ข้อควรระวังหรือ ปัญหาของ Docker

แม้ Docker จะช่วยให้การพัฒนาเป็นเรื่องง่ายขึ้นมาก แต่ก็มีบางจุดที่ควรระวังไว้ด้วย เช่น

| ปัญหาที่อาจเจอ | สาเหตุ / คำอธิบาย |
| ---------------- | ------------------- |
| ใช้พื้นที่ Disk เยอะ | การ build หลาย ๆ image หรือ container ที่ไม่ได้ลบออก จะกินพื้นที่มาก |
| Build ช้าในบางเครื่อง | ถ้าเครื่องมี RAM หรือ CPU น้อย การ build image ที่ซับซ้อนอาจใช้เวลานาน |
| Permission Error | บาง container ต้องใช้สิทธิ์ root หรือกำหนดสิทธิ์ volume ให้ถูกต้อง |
| Network ซับซ้อน | Container แต่ละตัวอยู่ใน network ของตัวเอง ต้อง config ให้เชื่อมกันได้ |
| เข้าใจยากช่วงแรก | Docker มีคำสั่งและแนวคิดเฉพาะตัว ต้องใช้เวลาเรียนรู้ให้เข้าใจจริง |

:::tip

ใช้คำสั่ง `docker system prune` เพื่อลบ container, image, และ cache ที่ไม่ได้ใช้แล้ว `docker system prune -a`

:::


## Session นี้จะพาทำอะไรบ้าง

ใน session นี้ เราจะลองสร้างโปรเจกต์ Node.js แบบง่าย ๆ แล้วรันใน Docker ตั้งแต่ต้นจนจบ

1. **ติดตั้ง Docker Desktop**  
   ดาวน์โหลดและติดตั้งจาก [docker.com](https://www.docker.com/)
2. **สร้างไฟล์ Dockerfile**  
   เพื่อกำหนดวิธี build image ของโปรเจกต์
3. **สร้าง Image และ Container**  
   ใช้คำสั่ง `docker build` และ `docker run`
4. **Docker Compose**  
   เพื่อรันหลาย container พร้อมกัน เช่น Node.js + PostgreSQL

เมื่อจบ session นี้ คุณจะเข้าใจตั้งแต่แนวคิดจนถึงการนำ Docker ไปใช้กับงานจริง


## Docker Images

**Image** คือแพ็กเกจแบบสำเร็จรูป (stand-alone package) ที่รวมทุกอย่างที่จำเป็นสำหรับการรันซอฟต์แวร์ตัวหนึ่งไว้ครบหมด  
ไม่ว่าจะเป็น **system command, code, runtime, library, environment หรือ configuration** — ทั้งหมดถูกบรรจุไว้ใน image เดียว  

จะมองว่า **image** เป็นเหมือน “พิมพ์เขียว (blueprint)” ของซอฟต์แวร์ก็ได้  
เวลาเราจะรันโปรแกรมจริง ๆ Docker จะนำ image นี้ไปสร้างเป็น **container** ซึ่งก็คือซอฟต์แวร์ที่กำลังทำงานอยู่จริงนั่นเอง  

> “Image คือแบบร่าง ส่วน Container คือของจริงที่กำลังรันอยู่”


## Docker Containers

**Container** คือสภาพแวดล้อมจำลองที่ถูกสร้างขึ้นมาจาก **Docker Image**  
จะมองว่าเป็น “ซอฟต์แวร์ที่กำลังรันอยู่จริง ๆ” ก็ได้ เพราะมันคือผลลัพธ์จากการนำ image มาทำงานนั่นเอง  

ภายใน container จะมีทุกอย่างครบเหมือนใน image — ทั้งโค้ด, ไลบรารี, และ environment ที่กำหนดไว้  
แต่จะแยกตัวจากระบบหลัก (host machine) ทำให้แต่ละ container ไม่ไปรบกวนกัน  

> **Image คือพิมพ์เขียว ส่วน Container คือบ้านที่สร้างขึ้นจากพิมพ์เขียวนั้น**  

เราสามารถสร้าง, รัน, หยุด, หรือลบ container ได้ง่าย ๆ ผ่านคำสั่ง Docker เช่น  
```bash
docker run
docker ps
docker stop
docker rm
```

## Getting Started with Docker

เริ่มต้นง่าย ๆ กับคำสั่งแรกของ Docker — **รัน “Hello World”**

```bash
docker run hello-world
```

![Docker Hello World](/img/hello-world.png)

## Docker Commands

คำสั่งพื้นฐานที่ควรรู้ในการใช้งาน Docker

### docker ps
ใช้สำหรับแสดง **container** ที่กำลังรันอยู่

```bash
docker ps
# docker ps -a แสดง container ทั้งหมด
docker ps -a 
```

### docker pull
ใช้สำหรับดาวน์โหลด **image** จาก Docker Hub (หรือ registry อื่น ๆ) มายังเครื่องเรา

```bash
# docker pull <image_name>
docker pull nginx 
```

### docker run 
ใช้สำหรับสร้างและรัน **container** จาก **image**

```bash
# docker run <image_name>
docker run nginx
```

### docker stop
ใช้สำหรับหยุด **container**

```bash
# docker stop <container_name> or <container_id>
docker stop nginx
```

## Dockerfile

**Dockerfile** คือไฟล์ที่ใช้บอกขั้นตอนในการสร้าง (**build**) Docker Image  
โดยในไฟล์นี้ เราจะกำหนดว่าต้องเริ่มจาก image อะไร และต้องติดตั้งหรือเตรียม environment ยังไงบ้าง  
เพื่อให้โปรเจกต์ของเราพร้อมรันใน container ได้ทันที  

พูดง่าย ๆ คือ  
> **Dockerfile คือสูตรทำเค้ก ส่วน Image คือเค้กที่อบเสร็จแล้ว 🍰**

### สร้าง express app

สร้างโปรเจกต์ Node.js แบบง่าย ๆ ด้วย express

```bash
npm init -y
npm install express
```

สร้างไฟล์ app.js

```javascript
const express = require('express')
const app = express()
const port = 3000

app.get('/', (req, res) => {
  res.send('Hello from Docker!')
})

app.listen(port, () => {
  console.log(`Server started on port ${port}`)
})
```

รัน app.js
```bash
node app.js
```

ลองเข้า `http://localhost:3000` ถ้าไม่ error จะได้ Hello from Docker!

สร้างไฟล์ **Dockerfile** สำหรับแอป Node.js:

```dockerfile
FROM node:20

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000

CMD ["node", "app.js"]
```

### สร้าง images ด้วย Docker build
```bash
docker build -t <image_name> .
```
อธิบายสั้น ๆ:
* `docker build` คือคำสั่งสำหรับสร้าง image จาก Dockerfile
* `-t` คือ tag หรือชื่อ image
* `.` คือ directory ที่มี Dockerfile

ตัวอย่าง:
```bash
docker build -t my-node-app .
```

```bash
Tao ❯ docker build -t my-node-app .
[+] Building 25.2s (11/11) FINISHED                                                                                                                                                                                                                     docker:desktop-linux
 => [internal] load build definition from Dockerfile                                                                                                                                                                                                                    0.0s
 => => transferring dockerfile: 188B                                                                                                                                                                                                                                    0.0s
 => [internal] load metadata for docker.io/library/node:20                                                                                                                                                                                                              6.5s
 => [auth] library/node:pull token for registry-1.docker.io                                                                                                                                                                                                             0.0s
 => [internal] load .dockerignore                                                                                                                                                                                                                                       0.0s
 => => transferring context: 2B                                                                                                                                                                                                                                         0.0s
 => [1/5] FROM docker.io/library/node:20@sha256:47dacd49500971c0fbe602323b2d04f6df40a933b123889636fc1f76bf69f58a                                                                                                                                                       16.0s
 => => resolve docker.io/library/node:20@sha256:47dacd49500971c0fbe602323b2d04f6df40a933b123889636fc1f76bf69f58a                                                                                                                                                        0.0s
 => => sha256:95255fad41c8b181ce4243b485583851644f140cfa3249a293b25643febce01a 1.25MB / 1.25MB                                                                                                                                                                          0.7s
 => => sha256:448a727bdd34b121bd1726b57e4d7f7912b44d75611b4042f11462d5fbecbd94 447B / 447B                                                                                                                                                                              1.1s
 => => sha256:204452dfeb8e1a30097dbb3ae26165c4786ccb9ef216628456c1d9f169bfa81a 48.37MB / 48.37MB                                                                                                                                                                        2.1s
 => => sha256:4403d4d53f4c623e00f85904acd8790e83ad890b47be6d5f2209e03cf43fae46 3.32kB / 3.32kB                                                                                                                                                                          3.9s
 => => sha256:e5bba4efb848c0bb1ec0e2278507ca2e3a6d4788f8cb73daf7b1066ce9d7fbb7 202.96MB / 202.96MB                                                                                                                                                                      6.9s
 => => sha256:020f0f7f102dcd1ca7603a86d7398adbe5369a820cc6f32954c0b3b5e2ac7403 64.37MB / 64.37MB                                                                                                                                                                        7.0s
 => => sha256:5463896571d3ff5317461a64229e9e4cb27d6d877114079419cf8b4fc96b0c02 23.60MB / 23.60MB                                                                                                                                                                        6.5s
 => => sha256:b81c3047c0240876c5be21e30ab0bb3930d31a1fc064a5cfe3b73eaec871a74c 48.36MB / 48.36MB                                                                                                                                                                        2.9s
 => => extracting sha256:b81c3047c0240876c5be21e30ab0bb3930d31a1fc064a5cfe3b73eaec871a74c                                                                                                                                                                               1.5s
 => => extracting sha256:5463896571d3ff5317461a64229e9e4cb27d6d877114079419cf8b4fc96b0c02                                                                                                                                                                               0.3s
 => => extracting sha256:020f0f7f102dcd1ca7603a86d7398adbe5369a820cc6f32954c0b3b5e2ac7403                                                                                                                                                                               1.6s
 => => extracting sha256:e5bba4efb848c0bb1ec0e2278507ca2e3a6d4788f8cb73daf7b1066ce9d7fbb7                                                                                                                                                                               4.1s
 => => extracting sha256:4403d4d53f4c623e00f85904acd8790e83ad890b47be6d5f2209e03cf43fae46                                                                                                                                                                               0.0s
 => => extracting sha256:204452dfeb8e1a30097dbb3ae26165c4786ccb9ef216628456c1d9f169bfa81a                                                                                                                                                                               1.3s
 => => extracting sha256:95255fad41c8b181ce4243b485583851644f140cfa3249a293b25643febce01a                                                                                                                                                                               0.0s
 => => extracting sha256:448a727bdd34b121bd1726b57e4d7f7912b44d75611b4042f11462d5fbecbd94                                                                                                                                                                               0.0s
 => [internal] load build context                                                                                                                                                                                                                                       0.1s
 => => transferring context: 2.73MB                                                                                                                                                                                                                                     0.1s
 => [2/5] WORKDIR /app                                                                                                                                                                                                                                                  0.8s
 => [3/5] COPY package*.json ./                                                                                                                                                                                                                                         0.0s
 => [4/5] RUN npm install                                                                                                                                                                                                                                               1.1s
 => [5/5] COPY . .                                                                                                                                                                                                                                                      0.2s
 => exporting to image                                                                                                                                                                                                                                                  0.3s 
 => => exporting layers                                                                                                                                                                                                                                                 0.2s 
 => => exporting manifest sha256:b00bdeb1a73497bc20611e89927fc5d4e7d00f18610dec9ebb6bfa30ba46bab1                                                                                                                                                                       0.0s
 => => exporting config sha256:99a61ce914bc992f577a031f245d021f78eec10c193284bd7245c2ded37f4009                                                                                                                                                                         0.0s
 => => exporting attestation manifest sha256:ff6b25e466d6152032196c7dcb3c17eb255a863bf51c81b715da79787e08629e                                                                                                                                                           0.0s
 => => exporting manifest list sha256:0e4a8e38ea8f82bc20ea77bd45558db08e368e3b93d82eddcecffee7020b2ca2                                                                                                                                                                  0.0s
 => => naming to docker.io/library/my-node-app:latest                                                                                                                                                                                                                   0.0s
 => => unpacking to docker.io/library/my-node-app:latest                                                                                                                                                                                                                0.1s

View build details: docker-desktop://dashboard/build/desktop-linux/desktop-linux/j9l2a7cqk1zfkv1sejjtncli5
```

### หลังจาก Build เสร็จ เราจะเห็น Image ที่สร้างขึ้น

เมื่อตัวคำสั่ง `docker build` ทำงานเสร็จแล้ว
เราสามารถตรวจสอบรายการ **Docker Image** ทั้งหมดในเครื่องได้ด้วยคำสั่ง:

```bash
docker images
```

```bash
REPOSITORY                           TAG                       IMAGE ID       CREATED         SIZE
my-node-app                          latest                    0e4a8e38ea8f   2 minutes ago   1.58GB
```

จากตารางด้านบน จะเห็นว่า Image ที่เราสร้างขึ้นล่าสุดคือ
`my-node-app:latest` ซึ่งถูกสร้างขึ้นเมื่อประมาณ 20 ชั่วโมงก่อน
เราสามารถนำ Image นี้ไปรันเป็น **Container** ได้ทันทีด้วยคำสั่ง:

```bash
docker run -p 3000:3000 my-node-app
```

:::tip
สังเกตได้ว่า Image ที่เรา Build ใหม่ ๆ มักจะมีขนาดค่อนข้างใหญ่ โดยเฉพาะถ้ามีขั้นตอนติดตั้ง dependency จำนวนมาก หรือ build จาก base image ที่ใหญ่ เพื่อแก้ปัญหานี้ เราสามารถใช้เทคนิค **Multi-stageBuild** ซึ่งช่วยให้เราสร้าง image หลายขั้นตอน (stage) แล้วคัดลอกเฉพาะไฟล์ที่จำเป็นไปยัง stage สุดท้าย ทำให้ขนาด image เล็กลงอย่างมาก
:::

> ตัวอย่างเช่น:
>
> ```dockerfile
> # Stage 1: Build source code
> FROM node:20-alpine AS builder
> WORKDIR /app
> COPY package*.json ./
> RUN npm install
> COPY . .
>
> # Stage 2: Run only the built app
> FROM node:20-alpine
> WORKDIR /app
> COPY --from=builder /app/app.js ./app.js
> COPY package*.json ./
> RUN npm install --only=production
> CMD ["node", "app.js"]
> ```
> ด้วยวิธีนี้ stage แรกจะถูกใช้เฉพาะตอน build เท่านั้น
> ส่วน stage สุดท้ายจะมีเฉพาะไฟล์ที่จำเป็นต่อการรันจริง
> **ผลลัพธ์คือ image มีขนาดเล็กลงมาก** และ deploy ได้เร็วขึ้น
>```
>REPOSITORY              TAG                      IMAGE ID       CREATED        SIZE
>my-node-app             latest                   2aa839505751   4 seconds ago  200MB
>```

## Docker Compose

Docker Compose คือเครื่องมือที่ใช้จัดการ หลาย container พร้อมกัน ผ่านไฟล์เดียวชื่อ docker-compose.yml
เราสามารถกำหนดว่าแต่ละ service ใช้ image อะไร, พอร์ตไหน, และเชื่อมต่อกันอย่างไร
แล้วสั่งรันทั้งหมดได้ในคำสั่งเดียว — docker compose up
ทำให้การพัฒนาและทดสอบระบบที่มีหลายส่วน (เช่น API + Database) ง่ายและเป็นระบบมากขึ้น.

ก่อนมี Docker Compose หากต้องรันโปรเจกต์ Node.js ที่ต้องพึ่ง PostgreSQL  
เราต้องรันคำสั่งยาว ๆ แบบนี้:

```bash
docker run -d --name db -e POSTGRES_USER=user -e POSTGRES_PASSWORD=pass postgres:15
docker run -d --name app -p 3000:3000 --link db:db my-node-app
```

:::tip

**Docker Compose** คือเครื่องมือที่ช่วยจัดการ **หลาย container พร้อมกัน**
ด้วยการกำหนดทุกอย่างไว้ในไฟล์เดียว

:::

สร้างไฟล์ docker-compose.yml

```yaml
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
  app:
    image: my-node-app
    ports:
      - "3000:3000"
    depends_on:
      - db
```

ลองรัน docker compose up -d
```bash
docker compose up -d
```

รัน docker ps
```bash
docker ps
```
จะได้ หน้าตาแบบนี้ขึ้นมา
```bash
CONTAINER ID   IMAGE         COMMAND                  CREATED         STATUS        PORTS                    NAMES
cbd47aaa3c24   my-node-app   "docker-entrypoint.s…"   2 seconds ago   Up 1 second   0.0.0.0:3000->3000/tcp   node-docker-app-1
ebd8d15dc189   postgres:17   "docker-entrypoint.s…"   2 seconds ago   Up 1 second   0.0.0.0:5432->5432/tcp   node-docker-db-1
```

Docker จะสร้าง network ให้โดยอัตโนมัติ
เชื่อมต่อทุก service เข้าหากัน และรันพร้อมกันในครั้งเดียว

---

### คำสั่งพื้นฐานที่ควรรู้
```
# สร้างและรันทุก service ตามไฟล์ docker-compose.yml
docker compose up

# รันแบบ background (detached)
docker compose up -d

# หยุดทุก service ที่รันอยู่
docker compose down

# ดู log ของ service ทั้งหมด
docker compose logs

# ดู container ที่รันอยู่ใน project นี้
docker compose ps

# rebuild image ใหม่ทั้งหมด
docker compose build --no-cache
```

:::tip

ใช้ `docker compose` (ไม่ใช่ `docker-compose`)
เพราะตั้งแต่ Docker รุ่นใหม่ ๆ ได้รวม Compose เข้ามาใน CLI หลักแล้ว
ไม่ต้องติดตั้งแยกต่างหากอีกต่อไป

:::


## Volumes คืออะไร
> Volumes คือพื้นที่เก็บข้อมูล (storage) ภายนอกของ container
> ใช้สำหรับ เก็บข้อมูลถาวร (persistent data)
> เช่น ฐานข้อมูล, ไฟล์ log, หรือไฟล์ที่ไม่อยากให้หายเมื่อ container ถูกลบ

โดยปกติแล้ว ถ้าเราไม่ได้ใช้ volume:
- ข้อมูลทุกอย่างที่อยู่ใน container จะหายหมดเมื่อเรารันคำสั่ง
```bash
docker stop && docker rm <container>
```
หรือเวลาที่ container ถูก rebuild ใหม่


### ตัวอย่างให้เห็นภาพ
เช่น เรามี PostgreSQL ที่รันใน Docker
ถ้าเราไม่ใช้ volume → เมื่อ container ถูกลบ ข้อมูลใน database จะหายหมด

แต่ถ้าเรากำหนด volume ไว้ใน docker-compose.yml แบบนี้
```yaml
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: mydb
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
```

Docker จะสร้าง storage ชื่อ db_data แยกไว้ในระบบ
และ mount เข้ามาใน path /var/lib/postgresql/data ของ container

> ผลลัพธ์คือ:
> - “Container เป็นของชั่วคราว แต่ Volume คือของถาวร”
> - ใช้ Volume เมื่อใดก็ตามที่ container ของคุณต้องเก็บข้อมูลไว้ใช้อีกในอนาคตครับ.
